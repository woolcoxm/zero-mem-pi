# Zero-Mem for pi — Design

Faithful reimplementation of **"Zero-Mem: Zero-Token Memory Operations for LLM Agents"** (Xiao et al., arXiv:2607.29377, 31 Jul 2026) as a [pi](https://pi.dev) coding-agent extension.

## The core insight (why this fits pi perfectly)

The paper's claim: *agent memory need not generate an intermediate representation of the past.* Most memory systems (MemGPT, Mem0, Zep, summaries) **burn LLM calls** to maintain memory. Zero-Mem keeps **raw interaction traces** and structures them — zero LLM calls for any memory operation; only the final answer uses the model.

pi already stores every message as a session entry. **Those entries ARE Zero-Mem's "interaction traces."** So this extension doesn't build a parallel store from scratch — it *organizes* pi's traces and *retrieves evidence per query*, injecting it before the model. That is exactly the paper's operating regime.

## Paper → pi mapping

| Paper component | This extension |
|---|---|
| **1. Provenance-preserving token-free substrate** | `message_end` event → capture unit (`MemoryStore.add`): role, text, `sessionId`, `cwd`, timestamp. Persisted to `~/.pi/agent/zero-mem/store.json`. |
| **NER (spaCy, non-generative)** | `makeExtractor`: `compromise` (if installed) **+ code-aware regex** (paths, identifiers, quoted terms, acronyms). Code regex matters more than NLP NER for a coding agent. |
| **2. Entity–context graph** (co-occurrence + adjacency) | `EntityGraph`: `entity → units` + `unit → entities` + idf-weighted unit adjacency (shared entities). Query = units mentioning query entities; closure = 1-hop neighbors. |
| **Graph propagation (PPR, Eq 8–10)** | `EntityGraph.ppr`: π ← (1−γ)r + γPᵀπ over the unit graph (γ=0.6); reset vector r seeded by direct entity matches + embedding-cosine entity matches (Eq 8, cached). v0.14. |
| **3. Temporal hierarchy** (session → episode → turn) | `retrieve`: BM25 over units **+ session-adjacent turn closure** (prev/next in same session). |
| **Query-conditioned routing** (ρ=0.6, primary/secondary) | Relational query (subject anchor, no temporal cues) → graph-primary; local/temporal → hierarchy-primary. Primary gets ρ, secondary 1−ρ (v0.14; was ±0.15 nudges). |
| **Dual-view retrieve + fusion** | Normalize each view to [0,1], fuse `wGraph·g + wHier·h`. |
| **Evidence closure** | Add 1-hop graph neighbors + adjacent turns at discounted weight. |
| **4. Deterministic calibration** | Filter (score>0.05, provenance scope, recency guard), dedupe, rank, cap top-K. |
| **Final-QA reader = the only LLM** | The normal pi agent turn. Evidence is injected via `before_agent_start → systemPrompt`. **Zero extra LLM calls.** |

## What we kept faithful vs. simplified (v0.1)

**Faithful:** raw-trace substrate, two-view structure (graph + temporal), query-conditioned routing, fusion, evidence closure, deterministic calibration, zero-LLM-call operations, final-reader-only LLM.

**Simplified for v0.1 (documented for v0.2):**
- **Entity graph** uses entity→unit mapping + on-the-fly shared-entity closure instead of precomputed pairwise co-occurrence weights (cheaper, same relational behavior at this scale).
- **Semantic signal** is BM25 (lexical) rather than a dense embedder. Still zero-LLM; an encoder (transformers.js / llama-server `/v1/embeddings`) is the natural v0.2 upgrade.
- **Answer-level calibration** (type/format checks on the reader's output) is deferred — it needs a typed task and is lower-value for open-ended coding.
- **Context-redundancy** avoidance uses a recency guard (skip the last ~2 min) instead of exact active-context set diffing.

## Hyperparameters (paper defaults)
- Routing ρ = **0.6**, closure discounts 0.35 (graph) / 0.25 (adjacent turn), top-K = **5**, min score 0.05. (`BM25`: k1=1.5, b=0.75.)

## Version status
- **v0.1** ✅ Raw-trace substrate, entity graph, temporal hierarchy, BM25, fusion, closure, calibration, zero-LLM injection.
- **v0.2** ✅ Dense semantic embeddings (`transformers.js` + `all-MiniLM-L6-v2`) replace word-matching for the timeline view, with automatic BM25 fallback. Proven on synonym queries that BM25 misses.
- **v0.3** ✅ Context-aware retrieval: fingerprints of the model's current window are passed in (`activeContext`) so we never inject what's already visible; plus a relevance floor (default 0.15) and near-duplicate de-duplication.
- **v0.4** ✅ Co-occurrence relational bridges (`EntityGraph.cooc`, `useBridges`): units whose entities co-occur with query entities earn graph score without a direct mention.
- **v0.5** ✅ Compact int8 embedding sidecar (`store.emb.bin`) + retention policy. Embeddings quantized to int8 (~21× smaller than inline JSON, cosine drift <0.002) live outside `store.json`; `maxUnits`/`maxAgeMs` bound growth. One-shot migration in `migrate.ts`. **This fixes store bloat + I/O, not per-request tokens** (injection size is unchanged).
- **v0.6** ✅ (1) Slimmer per-request injection — header 46→19 tok, default `topK` 5→3 / snippet 220→120ch (294→112 tok/turn, ≈1.9 s prefill saved on a 95 tok/s model). (2) HNSW ANN index over embeddings (pure-JS, gated at `hnswThreshold`=10000 where it starts to beat exact brute force; growth-gated rebuild so a per-message add never rebuilds). (3) Deterministic answer-level calibration (opt-in, non-destructive): code-fence / JSON / entity-coverage / verbatim-memory checks.
- **v0.7** ✅ (1) Stripped `tokens` from `store.json` (recomputed on load; text store ~25% smaller). (2) MMR diversity pruning for injection (`mmr`/`mmrLambda`; cuts redundant snippets). (3) Async background HNSW build (`HNSWIndex.buildAsync`, event-loop-yielding; `retrieve()` falls back to exact brute force until ready, so a >10k rebuild never stalls a turn). (4) Deterministic retrieval eval harness (`eval.ts`): recall@K / MRR ablation — semantic embeddings lift recall@5 0.75→0.96 vs BM25.
- **v0.8** ✅ Session-scoped recent-exclusion: `recentExcludeMs` now applies only to the *current* session (whose recent turns are still in the model's context window and already caught by `activeContext` fingerprints). Units from *other* sessions are always recallable, no matter how recent. Fixes a real bug where a fact the user just told the agent was unrecoverable in any new conversation opened within `recentExcludeMs` (default 2 min). See `test-recall.ts` (3/3).
- **v0.9** ✅ (1) Cross-project federation (`federate`/`federatePenalty`): when a project has nothing relevant above `minScore`, reach across to other projects (penalized, tagged `cross-project`); no leak when the project can answer itself. `test-federation.ts` (5/5). (2) Adaptive MMR λ (`adaptiveMmrLambda`): lookup queries → relevance-favoring, exploratory → diversity-favoring. `test-adaptive.ts` (5/5). (3) HNSW incremental insert (`HNSWIndex.add`): freshly-embedded units fold into the live index immediately, no waiting for the +50% rebuild. `test-incremental.ts` (7/7). (4) Retrieval eval on the **real LoCoMo10 benchmark** (`eval-locomo.ts`, 1986 QA): exposed that the v0.8 dense-only default UNDERPERFORMS BM25 on real data (r@5 0.273 vs 0.529); no naive fusion (max/weighted/RRF) recovers it. Fix: a **coverage router** — `scorePool` computes both BM25 + cosine and blends by query lexical coverage (`fusion:"coverage"`, default). Validated: LoCoMo r@5 0.534 (≥ BM25) AND paraphrase eval 0.92 (vs BM25 0.75). 38/38 tests green.

## Remaining roadmap
- **v0.10** ✅ End-to-end paper-metric eval (`eval-reader.ts`): LLM-reader token-F1 / exact-match / BLEU-1 on real LoCoMo10 — retrieve top-K (coverage), LLM answers at temp 0, score vs gold; per-category; degrades to retrieval hit-rate when no endpoint. On a 50-QA sample w/ a local Qwen3-Coder reader: F1 0.155 / EM 0.040 / BLEU-1 0.177 (strict RAG — top-5 snippets + session dates only — so a lower bound; the LoCoMo paper itself reports models lag humans). Context enriched with each snippet's SESSION DATE (LoCoMo answers are often dates derivable only from session metadata + relative words like 'yesterday'). **Determinism verified**: embeddings are bit-exact across runs (single-thread ONNX, drift 0) — the run-to-run "variance" seen during v0.9 tuning was a harness/env-propagation bug, not the model. evals take CLI args; LLM uses temp 0. Default **embedder upgraded MiniLM → bge-small-en-v1.5** (pure-semantic r@5 0.27→0.42; `Embedder.model` + `ZERO_MEM_EMBEDDER` override); the store persists an `embedder` stamp and auto-re-embeds on model change.
- **v0.11** (paper-fidelity audit vs arXiv:2607.29377) Structurally faithful — raw-trace substrate; entity-context graph + temporal-hierarchy dual view; query-conditioned routing ρ=γ=0.6; lexical BM25 + dense signals; evidence closure (Eq 14); top-K=5; **0 memory-operation tokens** (matches their Table 17). Investigated divergences: (a) **embedder** — paper uses **BGE-M3**; tested on LoCoMo: mean pooling r@5 **0.20** (worse than bge-small 0.42), CLS pooling discriminates better but full eval timed out >15min on CPU (paper used RTX 4090) → **keep bge-small**, made `Embedder(model,pooling)` configurable for opt-in BGE-M3+CLS. Paper's LoCoMo target (GPT-4o-mini reader): **F1 59.15 / BLEU-1 52.96**; our 0.155 gap is mostly the reader (chat vs coder) + bge-small vs BGE-M3.
- **v0.11** ✅ Three paper-fidelity fixes, all validated on LoCoMo (no regressions, 38/38 tests): (1) **min-max per-view normalization** (Eq 12) — replaces max-norm; stretches dense cosines [0.5,1]→[0,1]. Retrieval r@5 0.534→0.543, paraphrase 0.92→0.98. (2) **evidence calibration** (Eq 15) — answer-type compatibility re-rank (temporal→dates, quantity→numbers), default ON; MRR 0.396→0.404, r@1 0.291→0.304. (3) **top-10 retrieval budget** (paper ablation's best) in the evals; end-to-end F1 0.155→0.200 (BLEU-1 0.177→0.224), multi-session cat2 0.183→0.298. Production injection stays topK=3 (slim-token tradeoff, separate from the eval budget).
- **v0.12** ✅ Incremental-HNSW quality drift — MEASURED via `bench-hnsw-drift.ts` (build 5000, add 2000 incrementally): recall@10 **99.8% → 99.3%** over +40% growth (<1pp drift); a full rebuild over the same 7000 gives the identical 99.3%, confirming incremental `add` maintains graph quality. The existing +50% growth-gated rebuild is sufficient — **no fix needed**. (An earlier one-shot measurement showing 78% was a bench bug, not real drift.)
- **v0.13** ✅ Full-code audit fixes (26 new regression tests in `test-fixes.ts`; `npm test` now runs everything, 10 files / 67 assertions):
  - **HNSW stable IDs** — the index mapped nodes to unit *array indices*, which went stale whenever `enforceRetention` trimmed `units` (silent wrong-unit results at scale). Now maps to unit IDs; `searchUnitIds()` + a retrieve-time id→index map. Old bug repro is the first test in `test-fixes.ts`.
  - **Atomic persistence** — `store.json`/`emb.bin` now written temp+rename (no torn store on crash); a *failed* load sets `loadError` and `persist()` refuses to write (previously a corrupt store was silently overwritten with an empty one); `/memory-clear` is the documented reset.
  - **Cross-session merge** — all pi sessions share the store file; last-writer-wins persist erased the other session's captures. `persist()` now checks mtime and merges foreign units by id before writing.
  - **activeContext mismatch** — stored fingerprints were computed on truncated text (tool results capped at 500ch), so long content never matched the window's full-text fingerprints and got re-injected. `add(text, fpText?)` fingerprints the full text.
  - **Closure admission gate** — closure neighbors now obey session-scoped recent-exclusion and project scoping (cross-project federated hits can no longer pull their same-session neighbors in); sub-minScore candidates no longer *block* closure from rescuing the same unit at a discounted score.
  - **Bridge scale** — raw summed co-occurrence counts (50+) could drown direct entity hits (1–3) after min-max; bridge contribution is now `min(3, 1+log2(count))` (cheap stand-in for the paper's per-unit edge normalization, Eq 4).
  - **dequantize re-normalization** — int8 round-trip vectors are re-L2-normalized so HNSW's dot-product distance is metrically identical to brute-force cosine.
  - **Snippet sanitization** — injected snippets strip code fences/heading/bullet markers (stored tool output is untrusted; this removes prompt-structuring leverage on top of the existing "not authoritative" header).
  - **Env parsing** — `parseEnvNum`/`parseEnvNumOpt`: `0` respected, `""`/`abc` → default instead of NaN (`ZERO_MEM_MMR_LAMBDA=abc` used to silently disable MMR diversity).
  - **Misc** — embedder init failure no longer memoized (retry without restart); first-turn `embedAll` bounded to 100 units (a model change over a large store no longer embeds everything inside one turn); `migrate.ts` preserves the `embedder` stamp (was forcing a full re-embed after migration); path entity regex no longer matches "and/or"-style prose and now matches backslash Windows paths; `useClosure` retrieve option for eval ablations; dropped vestigial `onnxruntime-node` dep and the undeclared `typebox` runtime import (plain JSON schema).
  - **Eval honesty** — `eval-locomo.ts`: CORE's config is documented as the production pipeline (bridges+closure+calibration — it was mislabeled "pure ranking"), adds a closure-off ablation row and a paired McNemar significance test for CORE vs BM25 (the 0.014 margin was within noise); removed the brittle `BM25 ≥ semantic` assertion (an embedder property, not an invariant). `eval-reader.ts`: seeded random sample (was round-robin-first-per-conversation, category-biased) and prints ×100 paper-scale alongside 0–1 (the README used to juxtapose 0.200 with the paper's 59.15 across different scales). `test.ts` now asserts (was a print-only demo that exited 0 on failure).
- **v0.14** ✅ Paper-fidelity: **Personalized PageRank graph scoring** (Eq 8–10). Replaced v0.4's count-based direct matches + raw-summed co-occurrence bridges with a proper unit-level graph: edges idf-weighted by shared entities (Eq 4's per-context normalization; ubiquitous entities >10% of units skipped to avoid clique wash-out), query reset vector seeded by direct entity matches **plus embedding-cosine entity matching** (the paper matches query entities to graph entities by dense cosine — candidates token-filtered, embeddings cached in `Embedder.entityCache`), then π ← (1−γ)r + γPᵀπ power-iterated to convergence (γ=0.6, the paper's damping). Also **paper routing** (Eq 6–7/13): the query profile now picks the *primary* view (relational → graph-primary when a subject anchor exists, temporal → hierarchy-primary) and fuses ρ·primary + (1−ρ)·secondary — replacing v0.9's ±0.15 nudges that never implemented the asymmetric split. `useBridges:false` now means "direct entity matches only" (PPR off), enabling the ablation. Validated: 67/67 tests, LoCoMo r@5 0.543 → 0.546 (no regression; +0.003 is within noise — the significant McNemar p≈0.0009 margin vs BM25 comes from the coverage fusion, not the graph), paraphrase eval 0.98 held. Also: `eval-locomo.ts` reports a **held-out split** (convs 1–5 = historically tuned on; 6–10 = never tuned on) so tuned-on-test bias is visible, and `eval-hard.ts` is a new **200-fact hard-negative paraphrase eval** (every fact has a sibling differing in one value; 400 paraphrase queries) — FULL r@5 **0.695 vs true BM25 0.682** (margin +0.013; v0.14d fixed the baseline — the row previously mislabeled "BM25 only" was actually dense+graph at 0.647 because `hybrid:false` with a loaded embedder runs the v0.8 dense path, not BM25); the old 24-fact eval stays as a historical ablation only. Held-out split result (v0.14 run): CORE–BM25 margin is +0.015 on tuned convs 1–5 but **+0.007 on held-out 6–10** (0.531 vs 0.524) — the fusion generalizes; roughly half the headline gap was tuned-on-test bias.
- **v0.14b** ✅ Live-bug fix — the "what is your name?" confusion (memory injected "my name is mark." + "your name is Henry."; the reader hallucinated a conflict). Three root causes, three fixes:
  - **Pool-confidence gating**: min-max normalization (Eq 12) stretches ANY non-constant pool to [0,1], so a pool whose only matching term was "name" got its best tangential hit normalized to 1.0 and injected with a confident score. Each pool now carries a confidence = max(bmConf, semConf) — BM25 raw-best scaled by a corpus-size-aware anchor (1.2→4 with N; a fixed anchor made fresh small stores look dead), dense by (cosMax−0.55)/0.25 (bge: <0.55 unrelated, 0.8+ on-topic). The fused score is scaled ONCE by pool confidence (per-component scaling broke Eq 12's degenerate max==min⇒1 rule). Graph (PPR) mass is likewise corroborated: `wGraph·g·gConf`. Weak pools → nothing injected; "no memory" beats "confusing memory". The same gating correctly refuses to inject when a query matches every doc identically (idf≈0).
  - **Perspective compatibility** (`perspectiveCompat`, Eq-15 spirit): "my name" and "your name" queries are indistinguishable to BM25 and near-identical to bge (cos 0.71 vs 0.74 — measured). The discriminator is who speaks about whom: "your name?" ⇒ compatible only with a user-role unit that ASSIGNS a name ("your name is X", "I'll call you X" — asking isn't evidence); "my name?" ⇒ the user's own first-person statement is the source (+1), an assistant restatement is a weaker echo (−0.5). Incompatible evidence is capped below the injection floor.
  - **pi API robustness**: the live transcript showed same-session, seconds-old units being injected, meaning BOTH recency guards silently missed — `ctx.sessionManager?.getSessionId?.()` / `buildContextEntries?.()` return nothing on the actual host shape. `sessionIdOf()` now tries every shape (`sessionManager.getSessionId()`, `.session.id`, `.currentSession.id`, `ctx.session.id`, `ctx.sessionId`); `activeContextFingerprints()` likewise, and reads content under `message.content`/`content`/`text`/`message.text`.
  - **Follow-up (same day, second live bug)**: the agent had SELF-named ("I'm **Echo** — I named myself that last session") and "what is your name?" in a new conversation injected nothing — the strict user-only naming rule made self-naming evidence permanently incompatible. Verified END-TO-END against the real store (`~/.pi/agent/zero-mem/store.json`): reproduced 0 hits, then fixed. `perspectiveCompat` now accepts (a) user-role naming the assistant AND (b) assistant-role SELF-naming ("my name is X", "I'm called X", "call me X", "I named myself X"), while stale DENIALS ("I don't have a personal name", "no custom name set", "call me whatever") are explicitly incompatible — they were outranking the real evidence mid-fix. Post-fix on the real store: "what is your name?" → exactly one injected snippet, the Echo self-naming, denials suppressed. The verbatim transcript is now a regression test (`test-relevance.ts` §5).
  - **Follow-up 2 (third live bug — cross-project identity)**: pi started from `C:\Users\Robot\projects` (a session full of OBS/PowerShell output whose JSON contains literal `"name"` fields), while the identity facts live under the `zero-mem-pi` project. "whats my name?" scored the in-project OBS noise above `minScore`, so federation (which only fired on an empty in-project result) never ran — the user got OBS JSON injected and "I don't have any record of your name". Fix (v0.14c): **comparative federation** — the cross pool is also scored when in-project isn't decisively strong, and cross hits join when the best penalized cross score clears the best in-project score by +0.15 (a personal fact elsewhere outranks mild local noise; a project with its own strong answer still never leaks); plus **identity-query demotion** (`identityQuery`): for name/identity queries, units that are not identity statements (perspective 0) are scaled ×0.6, so JSON `"name"` fields stop winning "whats my name?". Verified E2E on a replica of the real two-project store: mark's unit tops "whats my name?", Echo tops "what is your name?", OBS queries still answer locally. Regression test: `test-relevance.ts` §6.
  Validated: new `test-relevance.ts` reproduces the exact transcripts (weak query ⇒ 0 injected; "what did i say my name was" ⇒ mark's statement top; unrelated facts never leak; Echo self-naming surfaces in a new conversation; denials suppressed), 11 files / 74 assertions green, hard-negative eval unchanged (0.695), easy eval 0.96.
- **v0.12** A stronger reader for the end-to-end eval (a chat model, not coder) to close the gap to the paper's LoCoMo F1 59.15 — our 0.200 is mostly reader-limited. `eval-reader` already accepts any OpenAI-compatible endpoint/model; load a chat LLM and re-run — **including graph/closure on–off ablations**: the retrieval metric (gold-in-top-K) can't credit supporting evidence, but reader F1 can (the paper's HotpotQA ablations: hierarchy-only −17.2 F1, closure off −4.2). That is the experiment that can credit or retire the structural views.

## Safety / cost
- **Zero extra LLM calls** in steady state (the paper's headline property). The optional `recall_memory` tool uses a normal tool round-trip but no extra model generation.
- Storage grows with history; text is capped at 4000 chars/unit (500 for tool results). Clear with `/memory-clear`.
- All handlers are try/caught so a malformed message can never break the agent.
